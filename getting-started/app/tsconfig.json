{
  "extends": "../tsconfig.base", /*specifies which config file this one extends*/
  "compilerOptions": {
    "removeComments": true,
    "outFile": "../js/app.js"

  },
  "files": [ /*array of the files we want to compile*/
    "./app.ts"

  ]
}